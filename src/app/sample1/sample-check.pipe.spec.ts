import { SampleCheckPipe } from './sample-check.pipe';

describe('SampleCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new SampleCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
